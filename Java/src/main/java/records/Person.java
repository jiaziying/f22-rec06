package records;

public record Person(String firstName,
                     String lastName,
                     String phoneNumber,
                     String zipCode,
                     String state,
                     String gender) {
}